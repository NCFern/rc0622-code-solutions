/* exported firstChars */

// return the string based on the length //
// use slice method to cut string from 0 to length //
// return string //

function firstChars(length, string) {
  var finalChar = string.slice(0, length);
  return finalChar;
}
