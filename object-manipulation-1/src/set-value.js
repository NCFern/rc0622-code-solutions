/* exported setValue */

// make a function that returns undefined //
// set the property of the object using bracket notation and it a new value //

function setValue(object, key, value) {
  object[key] = value;
}
