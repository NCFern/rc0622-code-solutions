/* exported isUpperCased */

// check if word is all uppercased. returns boolean //

function isUpperCased(word) {
  return word === word.toUpperCase();
}

// retun the word if it is strictly equal to the word all capitalized under the toUpperCase method //
