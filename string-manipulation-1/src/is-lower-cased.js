/* exported isLowerCased */

// check if word is all lowercased. returns boolean //

function isLowerCased(word) {
  return word === word.toLowerCase();
}

// retun the word if it is strictly equal to the word all lowercased under the toLowerCase method //
